const Footer = () => {
    return (
        <footer className="numisfera-footer">
            <div className="container">
                <p>&copy; {new Date().getFullYear()} Numisfera. Todos los derechos reservados.</p>
            </div>
        </footer>
    );
};

export default Footer;
